# Variables de entorno para Railway
# Agregar en Railway → proyecto → Variables

# Ruta de la base de datos SQLite (usar con Volume en /app/data)
DB_PATH=/app/data/imbio.db

# Puerto (Railway lo asigna automáticamente, no cambiar)
# PORT=8080
